# Local-only working artifacts (never committed)
docs/superpowers/

# Personal/org identity overlays generated by the installer
*.local
.zshrc.local
.gitconfig.local

# Backups the installer creates
*.bak.*
